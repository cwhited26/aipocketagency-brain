<!-- BEGIN AI POCKET AGENCY WORKSTATION -->

The authoritative system brain is `<brain-root>`. Read its `AGENTS.md` before non-trivial work,
then read the target repository's `CLAUDE.md` and `AGENTS.md`. The closest project instructions
control implementation.

The active workstation profile is `<profile-file>`. It defines the maximum permissions for this
machine. Check repository state before editing, preserve unrelated work, run the repository's
declared verification, and resolve secrets at use time through the configured secret manager.

<!-- END AI POCKET AGENCY WORKSTATION -->

