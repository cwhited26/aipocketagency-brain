# BEGIN AI POCKET AGENCY WORKSTATION

The authoritative system brain is `<brain-root>`. Before non-trivial work, read its `AGENTS.md`,
then the target repository's `AGENTS.md` and `CLAUDE.md`. Project-local instructions override
global defaults within that project.

The active workstation profile is `<profile-file>`. Treat its permissions as maximum authority,
not as a request to perform every allowed action.

Before changing a repository, run `git status --short`. Preserve unrelated work. Verify the exact
task with the repository's declared typecheck, tests, and production build before claiming it is
done. Never place secrets in prompts, files, logs, or commits; resolve them at use time through the
configured secret manager.

# END AI POCKET AGENCY WORKSTATION

